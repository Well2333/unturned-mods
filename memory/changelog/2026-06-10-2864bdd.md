---
date: 2026-06-10
commit: 2864bdd
type: docs
scope: repo
guideline_changed: true
---

# 同步 .NET SDK 安装状态（8.0.127 已装）

## 做了什么
- 之前的记录标注「仅装运行时、尚未安装 SDK」；现环境已安装 **.NET SDK 8.0.127**
  （`dotnet --list-sdks` 确认）。更新两处过时表述：
  - `CLAUDE.md`「当前环境状态」：改为已装 SDK 8.0.127，可直接 `dotnet build`/`pack`。
  - `memory/guidelines/development-standards.md` §5：去掉「尚需安装 SDK」。
- 用现装 SDK 验证：`dotnet build UnturnedMods.sln -c Release` → 0 警告 0 错误
  （当前解决方案仅含 `UnturnedMods.Shared`，尚无插件）。

## 为什么
- 环境实际状态已变化；项目规定环境/规范变化要及时同步，避免误导后续会话。

## 影响的文件 / 范围
- `CLAUDE.md`、`memory/guidelines/development-standards.md`。
- 历史变更记录 `2026-06-10-05bb583.md` 不回改（点对点历史记录，当时为真）。

## 注意事项 / 后续
- 仓库尚无实际插件，`src/plugins/` 仅有 `.gitkeep`。下一步可用
  `scripts/new-plugin.sh <PluginId> ["Display Name"]` 创建首个插件。

## 规范同步（guideline_changed = true）
- `development-standards.md` §5 构建前置条件由「尚需安装 SDK」改为「已装 SDK 8.0.127」。
